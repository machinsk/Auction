import Ember from 'ember';

export default Ember.Component.extend({
  // password: this.get('password');
  // passwordAgain: this.get('passwordAgain');
  // isMatching: Ember.computed(function(){
  //   return password === passwordAgain;
  // });
});
